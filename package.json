{
  "name": "fundacion-abanzando-landing",
  "private": true,
  "version": "1.0.0",
  "description": "Landing page de Fundación ABAnzando, desplegada como sitio estático en Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
